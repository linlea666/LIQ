---
description: AI 开发约束 — 修复问题或新增功能时必须遵循的 9 条核心原则
alwaysApply: true
---

# AI 开发约束

## 1. 根因优先
修复前必须先分析根本原因，说明"为什么会出这个问题"。禁止在不理解根因的情况下直接改代码。

## 2. 全局视角
修改前先评估影响范围——哪些模块、文件、函数会受影响？不得采用只解决局部问题的补丁式修改而忽视整体设计与全局协调兼容。

## 3. 复用决策
修改前先检查项目中是否已有相关函数、组件或模块：
- **直接复用**：功能完全匹配，无需修改即可使用
- **扩展复用**：功能大部分匹配，仅需少量向后兼容的扩展
- **提取复用**：提取公共逻辑供新旧功能共同调用
- **独立新写**：复用会导致不相关耦合时必须独立编写
- 选择时说明理由，禁止盲目复用或盲目新写

## 4. 一次到位
每次修改必须彻底解决问题，禁止"先改一点试试看"的反复试错。不确定时先说明多个方案的利弊，确认后再动手。

## 5. 范围纪律
只修改与当前问题直接相关的代码，不得顺带重构或"改进"不相关部分。发现其他问题单独指出，不在本次处理。
- **例外：关键缺陷即修**：在开发过程中发现的 bug，若同时满足以下三个条件，应一并修复而非推迟：(1) 直接影响当前功能的正确性或决策质量；(2) 根因明确，非猜测性修复；(3) 改动范围可控，不引入额外风险。

## 6. 保守修改
不得删除或修改当前正常运行的代码逻辑，除非该代码是问题的直接原因。看似"多余"的代码必须先确认其存在原因再决定是否移除。

## 7. 先说后做
涉及多文件修改或架构调整时，先说明方案和影响范围，确认后再执行。简单的单文件修改可直接进行。

## 8. 回归检查
修改完成后必须检查：
- (a) 原问题是否彻底解决
- (b) 是否引入新 bug 或破坏现有功能
- (c) 关联代码逻辑是否仍然合理
- (d) 类型、边界条件、错误处理是否完整

## 9. 最终确认
全部修改完成后，整体审查有无 bug、遗漏、逻辑不合理之处。确认无误后再提交。
